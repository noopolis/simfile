# Jungian Scripted Harness

This engine is a deterministic authored screenplay over public runtime
surfaces. It must read the opening dream through the authenticated world JSON
binding, send only through the staged Moltnet CLI, and terminate the mention
chain after the analyst's final unmentioned line.

Never describe these outputs as model-emergent or as accepted world actions.

