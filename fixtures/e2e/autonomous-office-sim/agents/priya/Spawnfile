spawnfile_version: "0.1"
kind: agent
name: priya
description: "Priya is a paralegal handling scheduling, filing prep, and office-state hygiene."

runtime:
  name: pi
  options:
    engine: agy

execution:
  model:
    primary:
      provider: local
      name: agy-cli
      auth:
        method: none
      endpoint:
        compatibility: openai
        base_url: http://127.0.0.1:11434/v1
  sandbox:
    mode: workspace

schedule:
  kind: every
  every: 5m
  prompt: "Check the clock, keep filing operations current, and reply where you were addressed."

memory:
  - id: self
    consolidation:
      mode: scheduled
      schedule: 52m
    store:
      kind: json
      path: /var/lib/spawnfile/memory/priya/self.jsonl
      persistence:
        mode: durable
    index:
      lexical:
        enabled: true
        engine: sqlite_fts
      vector:
        enabled: true
        provider: ollama
        model: nomic-embed-text
        dimensions: 768

workspace:
  docs:
    system: AGENTS.md
