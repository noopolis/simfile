spawnfile_version: "0.1"
kind: agent
name: jules
description: "Jules is Eleanor's teenager and appears in home conversations."

runtime:
  name: pi
  options:
    engine: claude

execution:
  model:
    primary:
      provider: local
      name: claude-cli
      auth:
        method: none
      endpoint:
        compatibility: openai
        base_url: http://127.0.0.1:11434/v1
  sandbox:
    mode: workspace

schedule:
  kind: every
  every: 8m
  prompt: "Check the clock, protect the theater thread, and reply where addressed."

memory:
  - id: self
    consolidation:
      mode: scheduled
      schedule: 37m
    store:
      kind: json
      path: /var/lib/spawnfile/memory/jules/self.jsonl
      persistence:
        mode: durable
    index:
      lexical:
        enabled: true
        engine: sqlite_fts
      vector:
        enabled: true
        provider: ollama
        model: nomic-embed-text
        dimensions: 768

workspace:
  docs:
    system: AGENTS.md
